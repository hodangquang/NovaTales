# Services Directory

Chứa các API client và các hàm gọi API hoặc tích hợp dịch vụ bên ngoài (Axios config, Fetch, Firebase SDK, v.v.).
- Ví dụ: `api.ts` (khởi tạo axios instance), `stories.ts` (gọi các API liên quan tới truyện).
