# Utils Directory

Chứa các hàm helper/utility thuần túy (pure functions) hỗ trợ xử lý dữ liệu chung.
- Ví dụ: `format.ts` (để format ngày tháng, tiền tệ), `storage.ts` (làm việc với AsyncStorage / MMKV), `validation.ts` (kiểm tra định dạng email, password).
